# Smart Nexus 智能售后顾问系统 —— 面试项目介绍

> 一个基于多 Agent 协同 + MCP 工具生态 + RAG 检索增强的 AI 原生应用，覆盖售后场景下的智能问答与维修网点导航。

---

## 一、项目背景

### 1.1 项目基本信息

| 项目项 | 内容 |
|--------|------|
| **项目名称** | Smart Nexus（智能售后顾问系统） |
| **项目类型** | 企业级 AI 原生应用（多 Agent + RAG + MCP） |
| **项目周期** | 2025.10 — 至今 |
| **个人角色** | 后端核心开发 / AI Agent 架构设计 |
| **技术形态** | 双 FastAPI 微服务 + Docker Compose + Nginx HTTPS + Electron 桌面端 |
| **代码规模** | 约 1.5 万行 Python，consultant + knowledge 双服务 |

### 1.2 项目解决的问题

传统售后服务存在三大痛点：

1. **人工坐席成本高、响应慢**——重复性咨询（产品参数、保修政策、维修网点）占比超 70%，人工排队体验差，夜间无值守。
2. **知识库零散、检索效率低**——联想 iKnow 等公开知识库内容详尽但站内搜索基于关键词，无法理解口语化问题，召回率低。
3. **跨域任务难以统一入口**——售后场景同时存在「知识问答」与「LBS 网点导航」两类异构任务，单一 FAQ 机器人无法跨域协作。

### 1.3 业务价值

- **降本**：AI 顾问 7×24 小时在线，预计承接 70%+ 一线咨询，显著降低人工坐席压力。
- **增效**：双路召回 RAG 使首次解决率（FCR）较传统关键词搜索大幅提升，端到端首字延迟 < 2s。
- **体验**：单一对话入口同时支持知识问答 + LBS 网点推荐，SSE 流式输出对标主流 ChatGPT 类产品。
- **可复用**：沉淀通用「三层 Agent 编排范式」+ 工具/路由双注册器抽象，新业务域（订单查询、保修登记等）可在 1 周内接入。

---

## 二、项目主要功能

| 功能模块 | 关键能力 |
|----------|----------|
| **智能流式对话（核心）** | SSE 三段式渲染：`THINKING`（原生思维链）/ `PROCESSING`（工具调用）/ `ANSWER`（最终答复） |
| **多 Agent 协同** | 三层 Agent 架构：意图路由 → 专家子 Agent → 工具调用 |
| **知识库问答（RAG）** | 双路召回（向量 + 标题关键词）+ MD5 去重 + 余弦重排 + 品牌中立 Prompt |
| **维修网点导航** | 百度地图 MCP 三级降级定位 + Haversine SQL 就近排序 |
| **联网检索兜底** | Tavily MCP，对知识库未覆盖问题进行 Web 搜索 |
| **鉴权与会话** | 手机号 + 验证码登录、JWT 软吊销、Redis 防爆破锁、3 轮上下文截断 |
| **知识库构建** | 独立 CLI：iKnow 增量爬虫 → 去重 → text-embedding-3-large 向量化 → Chroma |
| **全平台交付** | Docker Compose 一键部署 + Nginx HTTPS + Electron 桌面端 |

---

## 三、项目技术栈

| 分层 | 技术选型 |
|------|----------|
| **语言/框架** | Python 3.11、FastAPI、anyio、Pydantic v2、pydantic-settings |
| **AI 框架** | OpenAI Agents SDK（多 Agent 编排，`Runner.run_streamed` / `Runner.run` 双模式）、MCP 协议（Model Context Protocol，`MCPServerStreamableHttp`） |
| **大模型** | **MiniMax-M2.7-highspeed**（主 Agent + 子 Agent 均使用，OpenAI 兼容接口，支持 `enable_thinking` 原生思维链）；gpt-4o-mini（知识库生成）；text-embedding-3-large（向量化） |
| **检索/存储** | ChromaDB（向量库）、MySQL 8（业务库 + 网点 GIS）、Redis（验证码 / 登录锁）、jieba（中文分词） |
| **数据库连接** | DBUtils PooledDB（同步连接池）、aioredis（异步池）、`asyncio.to_thread` 异步包装 |
| **MCP 集成** | Tavily MCP（联网搜索）、百度地图 MCP（地理编码 + IP 定位） |
| **前端/客户端** | React + EventSource SSE、Electron 桌面端打包 |
| **部署运维** | Docker Compose、Nginx 443 SSL 反代、容器服务名互通 |

---

## 四、项目架构与模块

### 4.1 整体架构

```
        ┌─────────────────────────────────────────┐
        │       Electron 客户端 / Web 前端         │
        └────────────────┬────────────────────────┘
                         │ HTTPS / SSE
                ┌────────▼─────────┐
                │   Nginx (443)    │
                └────┬────────┬────┘
                     │        │
        ┌────────────▼──┐  ┌──▼──────────────┐
        │ consultant     │  │ knowledge       │
        │ :8001 顾问服务 │  │ :8000 知识库服务│
        │ 多Agent + MCP  │  │ RAG + 向量检索  │
        └────┬─────┬─────┘  └────┬─────────┬──┘
             │     │             │         │
        ┌────▼─┐ ┌─▼──┐      ┌───▼──┐  ┌───▼────┐
        │MySQL │ │Redis│     │Chroma│  │ MiniMax│
        └──────┘ └────┘      └──────┘  │ /OpenAI│
                                       └────────┘
                  ┌──────────┐   ┌────────────┐
                  │ Tavily   │   │ 百度地图   │
                  │  MCP     │   │  MCP       │
                  └──────────┘   └────────────┘
```

### 4.2 三层 Agent 架构（核心创新）

```
┌──────────────────────────────────────────────────┐
│  L1：coordination_agent（意图路由 / 主控）        │
│  - 模型：MiniMax-M2.7-highspeed，temperature=0.3  │
│  - max_turns=15（覆盖 ReAct 多轮 Thought→Action） │
│  - tools = agent_router_registry.routes()         │
│    主控对子 agent 完全透明，扩展子 agent 主控不动 │
└────────────┬─────────────────────────────────────┘
             │
   ┌─────────┴─────────┐
   │                   │
┌──▼──────────────┐  ┌─────▼────────────┐
│ L2：路由层       │  │ L2：路由层        │
│ AgentRouter      │  │ AgentRouter       │
│ Registry 闭包    │  │ Registry 闭包     │
│ 包装 FunctionTool + 同步 Runner.run()              │
│ 返回结构化 envelope:                              │
│ { status, summary, tool_calls, error_message? }   │
└──┬──────────────┘  └─────┬────────────┘
   │                       │
┌──▼──────────────┐  ┌─────▼────────────┐
│ L3：专家         │  │ L3：专家          │
│ consult_agent   │  │ navigation_agent  │
│ MiniMax-M2.7    │  │ MiniMax-M2.7      │
│ temperature=0   │  │ temperature=0     │
│ tools/mcp 全部  │  │ tools/mcp 全部    │
│ 走 ToolRegistry │  │ 走 ToolRegistry   │
└─────────────────┘  └──────────────────┘
```

**设计动机**：
- **L1 聚焦决策**：主 Agent 通过 `agent_router_registry.routes()` 拉取全量路由，避免「工具爆炸」导致路由混乱，对子 agent 完全透明；
- **L2 动态包装**：`AgentRouterRegistry.register(agent, description)` 内部用闭包把 `Runner.run()` 包装为标准 `FunctionTool`，并把子 agent 输出整理成 `{status, summary, tool_calls}` 结构化 envelope，便于主控判定 replan / 观察工具使用；
- **L3 专家执行**：子 Agent 通过 `tool_registry` 拉取全量工具与 MCP server，由 prompt 引导 LLM 自主选择，agent 定义里完全不硬编码工具清单（详见难点 7）。

### 4.3 模块划分（consultant 服务）

| 模块 | 路径 | 职责 |
|------|------|------|
| API 层 | `api/` | FastAPI 路由、CORS、JWT 中间件、MCP lifespan（asyncio.create_task 启动 60s 心跳） |
| Agent 层 | `agent/` | `master_agent.py` 主控、`node_agents.py` 专家、`agent_router.py` 提供 `AgentRouterRegistry`、`agent_engine.py` 流式编排引擎、`__init__.py` 集中注册子 agent 路由 |
| 服务层 | `service/` | `session_service.py` 历史会话查询/删除、`login_service.py` 登录鉴权 |
| 记忆管理 | `infra/memory/` | `MemoryManager`（历史读写 + 兜底分级截断）、`TokenBudgetCompressor`（三段式 LLM 压缩）、`MiniMaxSummarizer`（中段摘要器，7 分桶结构化输出）、`infra/memory/__init__.py` 装配 `default_compressor` |
| 异常处理 | `infra/error/` | `classifier.py`（`classify_llm_error()` HTTP 状态码→关键词→传输层三级分类）、`retry.py`（`jittered_backoff()` 带抖动指数退避） |
| 工具基础设施 | `infra/tools/base.py` | `BaseTool` 抽象基类（`execute → ToolResult`）+ `ToolRegistry` 注册器（同时管理本地工具与 MCP server） |
| 工具层 | `infra/tools/local/` | `retrieval_knowledge` / `search_coordinate_source` / `navigation_sites`，全部继承 `BaseTool` |
| MCP 层 | `infra/tools/mcp/` | Tavily / 百度地图 MCP 客户端 + 60s 心跳保活 + `trust_env=False` 隔离系统代理 |
| 数据访问 | `infra/db/`、`repo/` | MySQL 同步池（PooledDB）、Redis 异步池、仓储层 |
| 协议层 | `schema/` | SSE `StreamMessages` 流式响应模型、DTO |
| 通用工具 | `utils/tag_extract_utils.py` | `<think>` / `</think>` 标签流式拆分（增量字符级状态机） |

### 4.4 模块划分（knowledge 服务）

| 模块 | 职责 |
|------|------|
| 爬虫 CLI | 联想 iKnow 增量爬取，连续失败 5 次退避 60s |
| 向量化 CLI | 递归字符切片（chunk=3000，overlap=300），批量 batch=20 |
| 检索服务 | 双路召回（向量 + 标题关键词）→ MD5 去重 → 动态阈值（0.5）余弦重排 |
| 生成服务 | gpt-4o-mini 基于召回片段生成品牌中立回答 |

---

## 五、项目难点与解决方案

### 难点 1：MCP 长连接稳定性问题

**问题描述**：
百度地图、Tavily MCP 通过 HTTP Streamable 长连接通信，运行数小时后会被中间网关静默断开，但 SDK 内部 `_tools` 缓存（`cache_tools_list=True`）不会失效，导致后续工具调用阻塞超时，整条对话链路雪崩。

**解决方案**：
1. **主动心跳**：在 FastAPI lifespan 启动 60 秒心跳协程，定期调用 `list_tools()` 探活；
2. **强制走真实网络**：探活前 `server._tools = None`，避免命中本地缓存导致探活失效；
3. **失败自愈**：心跳失败立即 `cleanup() → connect()` 重连，全程无人工介入；
4. **代理隔离**：自定义 `httpx_client_factory(trust_env=False)`，避免容器继承宿主 `HTTP_PROXY/HTTPS_PROXY` 导致 TLS 握手失败；
5. **统一 cancel scope**：`anyio.run()` 替代 `asyncio.run()`，关闭服务时 cancel 信号在统一上下文里被处理，不泄漏到 asyncio 层。

**收益**：MCP 连接 7×24 小时稳定，故障平均自愈时间 < 60s。

---

### 难点 2：多 Agent 流式事件穿透与异常恢复

**问题描述**：
1. OpenAI Agents SDK 的 `Runner.run_streamed()` 事件流复杂（`raw_response_event` 含 `output_text` / `reasoning_text` / `reasoning_summary_text`，外加 `run_item_stream_event[tool_called]` 与 `agent_updated_stream_event` 等），若直接透传前端无法区分语义；
2. 部分模型不走独立 reasoning 通道，而是把思维链以 `<think>...</think>` 标签嵌在 `output_text.delta` 里，按字符增量到达，常常半个标签先到、半个后到；
3. LLM 偶发返回格式异常会中断 SSE 流，前端永久 pending。

**解决方案**：
1. **统一 SSE 协议**：自研 `StreamMessages` 模型，区分 `THINKING / PROCESSING / ANSWER` 三种 `render_type`，前端按类型渲染；
2. **think 标签流式拆分**：`tag_extract_utils` 实现增量字符级状态机，跨 chunk 拼接 `<think>` / `</think>`，把同一段 `output_text.delta` 拆成 `THINKING` / `ANSWER` 两类；切换 agent / 工具调用时强制 `flush` 残留 buf，避免标签夹断；
3. **工具/Agent 名汉化**：`TOOL_NAME_MAPPING` 与 `AGENT_NAME_MAPPING` 把英文转成「查询知识库」「咨询技术专家」等中文提示，前端体感更好；
4. **指数退避重试 + 兜底结束帧**：`stream_messages()` 递归重试最多 `MAX_TRY_COUNT=3` 次，每次按 `jittered_backoff(attempt, base_delay=0.5, max_delay=10.0)` 带抖动的指数退避，避免并发重试时的 thundering-herd；仅在第一帧未发出（`chunks_sent == 0`）时才重试，防止重复内容；期间向前端推送「系统异常 / 重新尝试」处理帧；最终失败发送 `EXCEPTION` 终止帧，前端不会僵死；
5. **异常分类短路**：`ValueError / TypeError / KeyError / AttributeError` 等代码错误直接 `EXCEPTION` 终止，不进重试链；LLM 调用异常经 `infra/error/classifier.py` 的 `classify_llm_error()` 按「HTTP 状态码 → message 关键词 → 传输层类型」三级流水线分类为 `auth / billing / rate_limit / overloaded / server_error / timeout / unknown`，由 `retryable` 字段决定是否退避重试。

---

### 难点 3：境外/复杂网络下的定位精度

**问题描述**：
网点导航强依赖用户位置，实际场景存在三种异常：① 地址输入俗称（如「北京西站附近」）；② 海外 VPN 用户 IP 解析失败；③ 容器内拿到的是反向代理 IP 而非用户真实 IP。

**解决方案 —— 三级降级定位**：

```
① map_geocode（地名解析，命中即返回）
    └─ 失败 → ② map_ip_location（IP 定位）
              └─ 内网 IP / 解析失败 → 通过 pystun3 获取真实公网 IP 重试
                                   └─ 仍失败 → ③ 北京坐标兜底（116.4133, 39.9109）
```

**配套**：API 层按 `X-Forwarded-For → X-Real-IP → client.host` 顺序透传 IP，保证 Nginx 反代后真实 IP 不丢；BD09 → WGS84 坐标系转换走 `coordinate_to_lng_lat`。

---

### 难点 4：RAG 召回率与品牌中立性

**问题描述**：
纯向量召回对**短查询、品牌词、型号编码**召回率低（如「Y9000P」）；联想知识库存在品牌倾向性表述，直接喂 LLM 会输出有偏内容。

**解决方案**：
1. **双路召回**：
   - 路 A：向量检索（Chroma L2 范数，top_5）—— 解决语义；
   - 路 B：标题关键词检索 —— 解决精确匹配；
     - 粗排：Jaccard 相似度（jieba 分词 70% + 字符集 30%），top_50；
     - 精排：标题向量余弦（70%）+ 关键词分（30%），top_5；
2. **MD5 去重**：以 `标题 + 内容前 100 字` 为指纹合并两路结果；
3. **动态阈值重排**：余弦相似度 0.5 为底线，低于则丢弃；
4. **Prompt 硬约束**：明确「品牌中立、忠实资料、图片仅以纯 URL 展示、结尾注明参考文档」四条硬性规则。

---

### 难点 5：JWT 重放攻击与多端登录互踢

**问题描述**：
传统 JWT 一旦签发无法吊销，用户在 A 设备登录后 B 设备无法强制下线，旧 token 被复用存在风险。

**解决方案**：
- JWT payload 写入 `iat`（签发时间戳），数据库 `user` 表存 `login_time`；
- 每次鉴权比对 `jwt.iat ≥ db.login_time`，**新登录刷新 `login_time` 使所有旧 token 立即失效**；
- Redis `login_lock:{phone}` 5 秒短锁防验证码爆破 + 重复登录；
- 验证码存 `phone_code:{phone}`（TTL 60s），`phone_code:{phone}:try` 限制错误次数。

**收益**：实现「软吊销」JWT，无需引入黑名单存储。

---

### 难点 6：上下文长度与对话连贯性的平衡

**问题描述**：
1. 完整保留历史会撑爆 token 预算；
2. 固定轮数截断会导致指代失败（"它的保修期是多久"丢失上文「Y9000P」）；
3. 长对话中 assistant 消息含有 `<think>` 思维链，若原样入库再次喂给模型会污染推理并浪费 token。

**解决方案——双层记忆架构（`infra/memory/`）**：

**主路：`TokenBudgetCompressor` 三段式 LLM 语义压缩**：
- **触发时机**：`save_history()` 时粗估 token 数（≈ 字符数 ÷ 3）≥ 8000 触发，不影响 SSE 首字节延迟；
- **三段保护**：
  - **head**：首条 user 消息作为任务/指代锚点，永不压缩；
  - **middle**：由 `MiniMaxSummarizer` 调主模型生成结构化摘要（用户目标/设备故障/位置导航/已检索知识/已回答问题/待办/用户偏好 7 分桶），替换原文；
  - **tail**：按 2000 token 预算从尾部反向累加，保护近期上下文不被压缩；
- **迭代更新**：摘要以 `[CONTEXT SUMMARY]` 前缀的 system 消息持久化在同一文件，下次压缩时传入 `previous_summary` 实现增量合并，多轮压缩不丢失早期事实；
- **降级保护**：压缩失败时降级为保留 head + tail 的纯截断，不阻塞主对话链路。

**兜底：`MemoryManager._truncate_history()` 四级分级截断**（无 LLM 依赖，纯结构性兜底）：
- ≤ 3 轮：全保留；
- 3–9 轮：保留最近 6 条；
- 9–18 轮：首条 user + 最近 6 条；
- > 18 轮：首条 user + 最近 3 条（进入此级通常意味着压缩失败）。

**入库前 `<think>` 剥离**：`_strip_think_from_messages()` 用正则清洗所有 assistant 消息的思维链标签，避免思维链回喂下一轮污染推理、浪费 token。

用户当前 IP 通过附加 user 消息透传到 agent（`[非用户问题，用户当前ip：xxx]`），不写入历史，不污染上下文。

---

### 难点 7：工具与子 Agent 的解耦扩展 ⭐

**问题描述**：
重构前，工具与子 Agent 都被硬编码到使用方：①工具用 `@function_tool` 散写、返回裸 `str`，无法承载 `is_error / metadata`；②每个子 agent 还需手写一个 `@function_tool route_xxx_agent(query)` 路由模板，且每个模板内手抄一遍 `Runner.run` + 异常处理。新增任何工具或子 agent 都要侵入 agent 定义文件，违反开闭原则。

**解决方案 —— 双层注册器抽象**（同模式落到工具与子 agent 两层）：

```
┌────────────────────────┐         ┌──────────────────────────┐
│ BaseTool（ABC）         │         │ AgentRouterRegistry       │
│  execute → ToolResult  │         │  register(agent, desc)    │
│  invoke() → FunctionTool│        │  闭包动态构造 FunctionTool│
└──────────┬─────────────┘         │  返回结构化 envelope:     │
           ▼                       │  status/summary/tool_calls│
┌────────────────────────┐         └──────────────┬───────────┘
│ ToolRegistry（单例）    │                        ▼
│  register / register_mcp│         ┌──────────────────────────┐
│  function_tools()       │         │ agent_router_registry    │
│  mcp_servers()          │         │  routes()                 │
└──────────┬─────────────┘         └──────────────┬───────────┘
           ▼ infra/tools/__init__.py              ▼ agent/__init__.py
   集中注册工具 + MCP                       集中注册子 agent 路由
           ▼                                       ▼
   sub_agent.tools = tool_registry.function_tools()
   sub_agent.mcp_servers = tool_registry.mcp_servers()
   coordination_agent.tools = agent_router_registry.routes()
```

**关键设计**：
1. **统一执行契约**：`BaseTool.execute(args: BaseModel) -> ToolResult`，Pydantic 校验入参，结构化返回（`output / is_error / metadata`）；
2. **适配器 + 闭包**：`BaseTool.invoke()` 把工具桥接为 `FunctionTool`；`AgentRouterRegistry.register()` 用闭包把 `Runner.run(agent)` 包装成 `route_{agent.name}` 形式的 `FunctionTool`，注册阶段纯声明、零调用，LLM 触发 tool_call 时才真正执行；
3. **结构化 envelope**：子 agent 调用结果统一返回 `{status: SUCCESS|ERROR, summary, tool_calls, error_message?}` JSON，主控可通过 `status` 判断是否 replan，通过 `tool_calls` 观测子 agent 实际用了哪些工具；
4. **集中装配**：两个注册器分别在 `infra/tools/__init__.py` 与 `agent/__init__.py` 完成 import 期装配，避免散落注册；
5. **Agent 透明化**：agent 定义里只写 `tools=registry.xxx()`，对具体工具/子 agent 完全无感知。

**收益**：新增工具/子 agent 从「改 agent 定义 + 写完整模板」简化为「写实现 + 一行 `register()`」；两层注册器同构，未来增加新业务域时主控代码完全不动。

---

### 难点 8：配置项失效兜底带来的隐性故障

**问题描述**：
Pydantic Settings 容易写成 `Field(default="…")`，环境变量未配置时进程仍能启动，结果是连到默认本地服务跑半天才发现「为什么调的是错的模型 / 错的库」。

**解决方案**：
- `API_KEY` / `BASE_URL` / `MAIN_MODEL_NAME` / `SUB_MODEL_NAME` 等核心字段去除 default；
- `@model_validator(mode="after")` 启动期 fail-fast，一次性把所有缺失字段拼成单条报错抛出，避免「修一个错暴露下一个错」的反复重启。

---

### 难点 9：主 Agent 由「一次性路由」升级为 ReAct 自主规划

**问题描述**：
原主 Agent 只做「意图识别 → 选一个子 agent 派发」的一次性路由：①子 agent 返回结果后无法基于结论自主衔接强相关的后续任务（如咨询专家提示「建议送修」却不能自动补一次导航）；②子 agent 失败时主控无从感知，要么透传异常要么静默吞掉；③隐式多任务必须靠用户多轮交互拆解。

**解决方案**：
1. **ReAct 循环固化进 Prompt**：用「思考 → 行动 → 观察 → 最终答复」四阶段模板改写 `coordination_agent.md`，并配 5 个正反示例（显式多任务 / 合理隐式推断 / 凭空推断禁止 / 条件性多任务等），让 LLM 学到判断标准；
2. **观察通道复用结构化 envelope**：直接消费难点 7 的 `{status, summary, tool_calls, error_message}`——`success` 取 `summary` 进入下一轮思考，`error` 换 query 重试 1 次或如实降级答复，严禁编造；
3. **推断边界刚性约束**：「触发器是观察而非猜想」、推断深度最多 +1 步、同一工具同一 query 禁止重复调用，配合 `max_turns=15` 强制收敛；
4. **基于观察重写 query**：上一轮观察提到"联想官方维修"，下一轮 navigation 的 query 就精化为「附近联想官方维修站」，让多 Agent 协作不只是串行调用、而是**信息逐步精化**；
5. **流程标记不外泄**：ReAct 流程词与过场白限制在 `<think>` 内，用户只看到工具进度条 + 最终综合答复。

**收益**：单轮即可串联链式任务、子 agent 错误可观测可重试、新业务域接入主控逻辑零改动。

---

## 六、项目成果与个人收获

### 6.1 项目成果

- 完成 consultant + knowledge **双服务** 从 0 到 1 的设计、开发与上线；
- 沉淀**通用三层 Agent 编排范式** + **工具/路由双注册器抽象**，新业务域/新工具均可零侵入接入；
- Docker Compose + Nginx HTTPS 一键部署，Electron 桌面端全平台覆盖；
- 知识库覆盖 2000+ 文档，平均检索耗时 < 1.5s，端到端首字延迟 < 2s；
- MCP 长连接 7×24 小时稳定运行。

### 6.2 个人收获

- 深度掌握 **OpenAI Agents SDK** 的多 Agent 编排、流式事件处理、`Runner.run_streamed` / `Runner.run` 双模式协同与异常恢复设计；
- 实战 **MCP 协议** 长连接保活、缓存陷阱、代理隔离等工程化细节；
- 落地 **RAG 双路召回** 工程范式，理解纯向量检索的局限与混合检索价值；
- 设计并落地**三段式 LLM 上下文压缩**（head 保护 + middle 摘要 + tail token 预算）+ 四级兜底截断，系统性解决长对话 token 控制问题；
- 建立**结构化 LLM 异常分类体系**（HTTP 状态码→关键词→传输层三级流水线 + jitter 退避），提升系统容错稳定性；
- 强化 **AI 原生应用** 端到端工程能力：Prompt 工程、Token 经济性、SSE 流式协议、`<think>` 标签流式拆分、容器化部署。

---

> 本项目深度实践了 **多 Agent 协同、MCP 工具生态、流式 SSE、RAG 双路召回、容器化运维** 等前沿方向，是一个典型的「AI 原生应用」端到端工程化案例。
